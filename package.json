{
  "name": "memory-app-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build": "npm run build -w backend",
    "prestart:prod": "npm run build && npx -y kill-port 3000",
    "start:prod": "npm run start:prod -w backend"
  }
}
